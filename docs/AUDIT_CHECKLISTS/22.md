# Audit checklist — PR #22 (Sprint 1A.5 — setup avançado dedicado)

> Use ao revisar o PR que o Cloud Code abre depois de executar prompt #22.
> **Sem dependência cruzada.** Refactor visual puro — zero schema novo, zero migration.

## Estrutura

- [ ] Branch `feat/1A5-setup-avancado`, base `main`
- [ ] CI verde
- [ ] Mergeable
- [ ] `Package.resolved` NÃO tocado
- [ ] Sem migration: `git diff main..HEAD --stat -- 'supabase/migrations/*' 'ios/p1fast-core/Sources/P1FastCore/Persistence/Migrations.swift'` → vazio

## Files esperados

**Criados:**
- `ios/p1fast-ios/Sources/Views/SetupAvancadoView.swift`

**Editados:**
- `ios/p1fast-ios/Sources/Views/CarroModalView.swift` (botão "Setup avançado" no header → push pra SetupAvancadoView)
- `ios/p1fast-ios/Sources/Views/ContentView.swift` (launch arg `--p1-setup-avancado`)

**NÃO devem aparecer:**
- `CarroSetupOverrides.swift` (modelo já existe, não tocar)
- Migrations / SQL
- Repositories (CarroRepository já tem upsert/load)
- Outras views

## Visual (5 grupos canônicos)

- [ ] Visual <2% diff vs `_design-reference/mockup-setup-avancado.html`
- [ ] Grupos:
  - PNEUS (4 inputs: pressão DE/DD/TE/TD)
  - ALINHAMENTO (3 inputs: cambagem D/T, convergência T)
  - SUSPENSÃO (3 inputs: mola D/T, altura D)
  - FREIOS (1 input: bias dianteiro)
  - MOTOR · TRANSMISSÃO (3 inputs: combustível, mapa, diferencial)
- [ ] Tipografia 18pt (vs 16pt do modal inline)
- [ ] Setas de navegação entre grupos

## Persistência

- [ ] Lê e escreve o MESMO `configuracoes.overrides` (JSON do CarroSetupOverrides)
- [ ] Não duplica modelo de overrides
- [ ] Footer "Cancelar / Salvar" — Cancelar volta sem persistir; Salvar grava + fecha
- [ ] Idêntico ao padrão do CarroModalView (consistência)

## Smoke

```bash
cd ios/p1fast-core && swift run p1fast-smoke
xcodebuild ... build

DBPATH=$(find ~/Library/Developer/CoreSimulator/Devices -name "*.sqlite" -path "*p1fast*" 2>/dev/null | head -1)

# Editar override pelo modo avançado:
sqlite3 "$DBPATH" "SELECT overrides FROM configuracoes WHERE carro_id='<id>';"
# Esperado: JSON atualizado

# Editar pelo modal inline → abrir avançado → reflete?
# (manual no simulator)
```

## Screenshots em `/tmp/p1-setup-avancado/`

- `setup-grupo-pneus.png`
- `setup-grupo-suspensao.png`
- `setup-todos.png`
- `setup-edit-flow.png` (antes/depois)

## Decisão pós-merge

Sprint 1A.5 fechado. Próximo: #23 (HTTP transport — Sprint 1A.6). Verificar em `docs/SPRINT_1A6_FINISH_PROMPTS.md` que pré-requisito Supabase prod está OK (já estava em 2026-05-03, vide HANDOFF).
