# Audit checklist — PR #18 (Sprint 1A.4 — pessoas schema v2)

> Use ao revisar o PR que o Cloud Code abre depois de executar prompt #18.
> **blockedBy:** #16 (precisa das colunas `altura_cm`/`peso_kg`/`nascimento` em `pilotos`/`passageiros`).
> **Paralelizável com:** #17 (zonas independentes).

## Estrutura

- [ ] Branch `feat/1A4-pessoas-v2`, base `main` (com #16 já mergeado)
- [ ] CI verde: node-smoke + swift-smoke
- [ ] Mergeable
- [ ] `Package.resolved` NÃO tocado
- [ ] Sem migration nova (#16 já criou as colunas)

## Files esperados (e SÓ esses)

**Editados:**
- `ios/p1fast-ios/Sources/Views/PilotoCadastroView.swift`
- `ios/p1fast-ios/Sources/Views/PassageiroCadastroView.swift`
- `ios/p1fast-ios/Sources/Persistence/PilotoRepository.swift`
- `ios/p1fast-ios/Sources/Persistence/PassageiroRepository.swift`

**Files que NÃO devem aparecer no diff:**
- Migrations / SQL
- Models.swift (#16 já adicionou os campos nas structs)
- StintModalView, ContentView (out of scope)

## Forms (3 campos novos por view)

- [ ] PilotoCadastroView e PassageiroCadastroView, em ambas:
  - "Altura (cm)" — input number, opcional
  - "Peso (kg)" — input number 1 decimal, opcional
  - "Data de nascimento" — DatePicker `.date` (sem hora), opcional
- [ ] HelperNote "altura/peso/idade virão depois" REMOVIDA (se existia).
- [ ] Edição pré-preenche valores atuais (NULL → input vazio, não chutar default).

## Validação

- [ ] alturaCm: 100 ≤ x ≤ 230, senão border red + msg "Altura parece fora do esperado".
- [ ] pesoKg: 30 ≤ x ≤ 200, idem.
- [ ] nascimento: 1900-01-01 ≤ d ≤ hoje, idem.
- [ ] Campos vazios são VÁLIDOS (todos opcionais) — submit funciona com NULL.

## Repositories

- [ ] `PilotoRepository.upsert` aceita os 3 campos novos.
- [ ] `PassageiroRepository.upsert` idem.
- [ ] Persiste como Int (cm), Double (kg), Int64 (epoch unix segundos do dia de nascimento).

## Smoke local

```bash
cd ios/p1fast-core && swift run p1fast-smoke
# Esperado: 158/0
```

## Smoke persistência

```bash
DBPATH=$(find ~/Library/Developer/CoreSimulator/Devices -name "*.sqlite" -path "*p1fast*" 2>/dev/null | head -1)

# Cadastrar piloto novo com os 3 campos:
sqlite3 "$DBPATH" "SELECT nome, altura_cm, peso_kg, nascimento FROM pilotos ORDER BY created_at DESC LIMIT 1;"
# Esperado: row com 3 valores não-NULL

# Cadastrar piloto SÓ com nome:
sqlite3 "$DBPATH" "SELECT altura_cm, peso_kg, nascimento FROM pilotos WHERE nome='<nome>';"
# Esperado: NULL | NULL | NULL

# Editar Flavio Marx (seedado) — adicionar altura:
sqlite3 "$DBPATH" "SELECT altura_cm FROM pilotos WHERE id='piloto-mock-flavio';"
# Esperado: valor preenchido

# Mesmo teste pra passageiros
sqlite3 "$DBPATH" "SELECT altura_cm, peso_kg, nascimento FROM passageiros ORDER BY created_at DESC LIMIT 1;"
```

## Screenshots esperados em `/tmp/p1-pessoas-v2/`

- `piloto-cadastro-3-campos.png`
- `piloto-cadastro-preenchido.png`
- `piloto-cadastro-validacao.png` (altura=300, border red)
- `passageiro-cadastro-3-campos.png`

## Decisão pós-merge

Próximo prompt: qualquer um do conjunto {#19, #20, #21, #22}. Sprint 1A.4 fechado quando #17 + #18 mergearem. Sugestão: #19 (trechos) — independente do schema v3 que #20/#21 trazem.
