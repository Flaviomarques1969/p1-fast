# Audit checklist — PR #20 (Sprint 1A.5 — catálogo de lições)

> Use ao revisar o PR que o Cloud Code abre depois de executar prompt #20.
> **Sem dependência cruzada.** Cria migration nova (0004 + GRDB v4_licoes).

## Estrutura

- [ ] Branch `feat/1A5-licoes`, base `main`
- [ ] CI verde
- [ ] Mergeable
- [ ] `Package.resolved` NÃO tocado

## Migration naming (atenção)

- [ ] GRDB: `m.registerMigration("v4_licoes")` — vai DEPOIS de `v2_pull_cursor` e `v2a_columns` (ordem de registro define ordem de aplicação)
- [ ] Postgres: arquivo `supabase/migrations/0004_licoes.sql` (número reservado no HANDOFF)
- [ ] NÃO usar `0006` ou outro — `0006` é do #16

## Files esperados

**Criados:**
- `ios/p1fast-ios/Sources/Views/LicaoListaView.swift`
- `ios/p1fast-ios/Sources/Persistence/LicaoRepository.swift`
- `supabase/migrations/0004_licoes.sql`

**Editados:**
- `ios/p1fast-core/Sources/P1FastCore/Persistence/Migrations.swift` (+ registerMigration `v4_licoes`)
- `ios/p1fast-core/Sources/P1FastCore/Persistence/Models.swift` (+ struct Licao)
- `ios/p1fast-ios/Sources/Views/ContentView.swift` (entrypoint sub-tab "Lições" + launch arg `--p1-licoes`)

**NÃO devem aparecer:**
- DB.swift (sem mudança — register é monolítico)
- 0005 ou outras migrations
- `src/data/lesson-library.js` (legacy, intacto)

## Schema

```sql
-- Esperado em 0004_licoes.sql + GRDB
CREATE TABLE licoes (
    id TEXT PRIMARY KEY,
    titulo TEXT NOT NULL,
    descricao TEXT,
    categoria TEXT NOT NULL,
    nivel TEXT NOT NULL,
    fase TEXT,
    tipo_curva TEXT,
    sinais_requeridos TEXT,
    ativa INTEGER NOT NULL DEFAULT 1,
    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL,
    synced_at INTEGER
);
```

(Postgres: substituir `INTEGER` ON timestamps por `bigint` e `TEXT` por `text`, `ativa INTEGER NOT NULL DEFAULT 1` → `ativa boolean NOT NULL DEFAULT true`.)

## Seed canônico (12 lições)

- [ ] 7 MVP **ativas** (`ativa=1`)
- [ ] 5 Fase 2 **inativas** (`ativa=0`)
- [ ] Nomes/categorias/níveis literais de `src/data/lesson-library.js` — confirmar com:
  ```bash
  grep -c "id:" src/data/lesson-library.js
  # Esperado: 12 (ou número que bate com o JS atual)
  ```
- [ ] Bootstrap idempotente: count==12 → no-op; count != → upsert por id mantendo `ativa` flag

## UI

- [ ] Sub-tab "Lições" dentro de "Cadastros" (BottomNav slot já renomeado em #15)
- [ ] Toggle "Só ativas" no header (default ON)
- [ ] "Todas" mostra 12, com badge "Fase 2" nas 5 inativas
- [ ] Tap no row abre sheet de detalhe (descrição + sinais + fase/tipo curva). Read-only.

## Smoke

```bash
cd ios/p1fast-core && swift run p1fast-smoke
xcodebuild ... build

DBPATH=$(find ~/Library/Developer/CoreSimulator/Devices -name "*.sqlite" -path "*p1fast*" 2>/dev/null | head -1)
sqlite3 "$DBPATH" "SELECT COUNT(*), SUM(ativa) FROM licoes;"
# Esperado: 12|7

sqlite3 "$DBPATH" "SELECT titulo, categoria, ativa FROM licoes ORDER BY ativa DESC, categoria;"
# Esperado: 7 ativas + 5 inativas
```

## Screenshots em `/tmp/p1-licoes/`

- `licoes-ativas.png` (toggle ON, 7 rows)
- `licoes-todas.png` (12 rows, badges Fase 2)
- `licao-detalhe.png` (sheet)

## Action pós-merge

Aplicar 0004 em prod Supabase:
```bash
SUPABASE_DB_PASSWORD='BeBZzfPVtiNXKGUojTK8c8BnEx6G' supabase db push --linked
```

## Decisão pós-merge

Próximo: #21 (pendências) — paralelizável foi com este, ou já mergeado.
