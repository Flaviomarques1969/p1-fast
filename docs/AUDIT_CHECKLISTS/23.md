# Audit checklist — PR #23 (Sprint 1A.6 — HTTP transports + Reachability + SyncCoordinator)

> Use ao revisar o PR que o Cloud Code abre depois de executar prompt #23 (sub-prompt C).
> **Pré-requisito de execução:** Supabase prod existente (já existe — project ref `fvhwltzhytpnhlqbttmd`, vide HANDOFF) + `.env.xcconfig` populado por Flávio antes do build.

## Estrutura

- [ ] Branch `feat/1A6-http-transport`, base `main`
- [ ] CI verde
- [ ] Mergeable
- [ ] `Package.resolved` NÃO tocado
- [ ] `.env.xcconfig` (com creds reais) NÃO commitado: `git ls-files | grep "\.env\.xcconfig$"` → vazio (só `.example`)
- [ ] `.env.xcconfig` listado em `.gitignore`

## Files esperados

**Criados:**
- `ios/p1fast-ios/Sources/Sync/URLSessionSyncTransport.swift`
- `ios/p1fast-ios/Sources/Sync/URLSessionPullTransport.swift`
- `ios/p1fast-ios/Sources/Sync/URLSessionTelemetryTransport.swift`
- `ios/p1fast-ios/Sources/Sync/Reachability.swift`
- `ios/p1fast-ios/Sources/Sync/SyncCoordinator.swift`
- `ios/p1fast-ios/Sources/Configuration.swift`
- `ios/p1fast-ios/.env.xcconfig.example`

**Editados:**
- `ios/p1fast-ios/p1fast-ios/Info.plist` (+ `SUPABASE_URL` + `SUPABASE_ANON_KEY` via `$(VAR)`)
- `ios/p1fast-ios/.gitignore` (confirmar `.env.xcconfig`)
- `ios/p1fast-ios/Sources/Views/ContentView.swift` (instancia SyncCoordinator + injeta como `@EnvironmentObject`)
- `ios/p1fast-ios/p1fast-ios.xcodeproj/project.pbxproj` (referência ao xcconfig nas Build Configurations Debug + Release)

**NÃO devem aparecer:**
- `p1fast-core/Persistence/SyncDrainer.swift` (lógica pura, congelada)
- `p1fast-core/Persistence/PullExecutor.swift` (idem)
- `p1fast-core/Persistence/TelemetryUploader.swift` (idem)
- `supabase/functions/` (Edge já entregue em sub-prompt A)
- Repositories
- Migrations

## Transports

- [ ] `URLSessionSyncTransport` → `POST {SUPABASE_URL}/functions/v1/sync` com `Authorization: Bearer {jwt-or-anon}`
- [ ] `URLSessionPullTransport` → `GET {SUPABASE_URL}/functions/v1/pull` (com cursor)
- [ ] `URLSessionTelemetryTransport` → `POST {SUPABASE_URL}/functions/v1/ingest`
- [ ] Conform aos protocols definidos em p1fast-core (NÃO duplicar protocol)

## Reachability

- [ ] Wrapper `NWPathMonitor` em ator dedicado
- [ ] `@Published var isReachable: Bool` ou stream Combine
- [ ] Transição offline→online dispara callback registrável

## SyncCoordinator

- [ ] Orquestra 3 drainers (sync + pull + telemetry)
- [ ] Acordado por: timer 30s, reachability change, trigger manual após mutation
- [ ] Sync drain: 30s + reachability + on mutation
- [ ] Pull drain: boot + 5min idle
- [ ] Telemetry drain: 60s + on stint termina
- [ ] Backoff via `BackoffPolicy.swift` (já existente)
- [ ] Erro silencioso V1 (log + retry, sem alert UI — UI vem em #24)

## Configuração / segredos

- [ ] `Configuration.swift` lê de `Bundle.main.object(forInfoDictionaryKey:)`
- [ ] `.env.xcconfig.example` com placeholders (não creds reais)
- [ ] Build sem `.env.xcconfig` real ainda DEVE compilar (com URL vazia → coordinator entra em estado .offline e loga)

## Smoke

```bash
cd ios/p1fast-core && swift run p1fast-smoke
# Esperado: 158/0 (transports não testados aqui — só mocks)

xcodebuild ... build
# Esperado: SUCCEEDED com .env.xcconfig criado localmente

# Smoke E2E manual (com creds reais):
# 1. Cadastrar 1 piloto pelo simulator
# 2. Aguardar 30s
# 3. Conferir no Supabase REST:
curl -H "apikey: $SUPABASE_ANON_KEY" \
  "${SUPABASE_URL}/rest/v1/pilotos?select=*&order=created_at.desc&limit=1"
# Esperado: row recém-criada

# Confirmar .env.xcconfig real NÃO foi commitado:
git ls-files | grep "\.env\.xcconfig$"
# Esperado: vazio
```

## Screenshots em `/tmp/p1-sync/`

- `app-boot-coordinator.png` (console "SyncCoordinator started")
- `network-online-trigger.png`
- `mutation-trigger.png`

## Decisão pós-merge

Próximo: #24 (UI Sincronização) — depende de SyncCoordinator estar injetado.
