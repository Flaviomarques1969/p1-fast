# Audit checklist — PR #16 (Sprint 1A.4 — schema v2)

> Use ao revisar o PR que o Cloud Code abre depois de executar prompt #16.

## Estrutura

- [ ] Branch é `feat/1A4-schema-v2`, base `main`
- [ ] CI verde: node-smoke + swift-smoke
- [ ] Mergeable
- [ ] `Package.resolved` NÃO tocado: `git diff main..HEAD --stat -- ios/p1fast-core/Package.resolved` → vazio

## Files esperados (e SÓ esses)

**Editados:**
- `ios/p1fast-core/Sources/P1FastCore/Persistence/Migrations.swift`
- `ios/p1fast-core/Sources/P1FastCore/Persistence/DB.swift`
- `ios/p1fast-core/Sources/P1FastCore/Persistence/Models.swift`
- `ios/p1fast-ios/Sources/Persistence/StintRepository.swift`
- `ios/p1fast-ios/Sources/Views/ContentView.swift`
- `ios/p1fast-ios/Sources/Views/StintModalView.swift`

**Criado:**
- `supabase/migrations/0006_v2_schema_columns.sql`

**Files que NÃO devem aparecer no diff:**
- Outras tabelas do Models.swift (Pneu, Combustivel, Carro etc — sem mudança esperada)
- Edge Functions
- Migrations 0001/0002/0003 existentes
- Outras views

## Schema v2 (verificar via grep no diff)

```bash
git diff main..HEAD -- ios/p1fast-core/Sources/P1FastCore/Persistence/Migrations.swift | grep -E "ADD COLUMN.*pneu_id|ADD COLUMN.*combustivel_id|ADD COLUMN.*qt_combustivel_litros|ADD COLUMN.*altura_cm|ADD COLUMN.*peso_kg|ADD COLUMN.*nascimento" | wc -l
# Esperado: >= 9 (9 ALTER TABLE no total)
```

## Cleanup de aliases (verificar nenhum caller órfão)

```bash
grep -rn "StintRepository.pilotoFlavioId\|StintRepository.pilotoBrunoId" ios/ --include="*.swift"
# Esperado: VAZIO (zero callers)
```

## Migration naming

- [ ] GRDB: método chama-se `v2a_columns` (não `v2`, porque já existe v3 RLS fix em main)
- [ ] Postgres: arquivo `0006_v2_schema_columns.sql` (NÃO `0002`, NÃO `0003`)
- [ ] DB.swift registra `Migrations.v2a_columns(&migrator)` ANTES de `Migrations.v3_*` (ordem lógica)

## Smoke local

```bash
cd ios/p1fast-core && swift run p1fast-smoke
# Esperado: 158/0
```

## Smoke migration upgrade

```bash
DBPATH=$(find ~/Library/Developer/CoreSimulator/Devices -name "*.sqlite" -path "*p1fast*" 2>/dev/null | head -1)
sqlite3 "$DBPATH" ".schema sessoes" | grep -cE "pneu_id|combustivel_id|qt_combust"
# Esperado: 3

sqlite3 "$DBPATH" ".schema pilotos" | grep -cE "altura_cm|peso_kg|nascimento"
# Esperado: 3
```

## Action pós-merge (ação manual do Flávio, NÃO do Cloud Code)

Após merge em main, aplicar migration 0006 em prod Supabase:

```bash
cd /Users/imac/Projetos/P1\ Fast
supabase link --project-ref fvhwltzhytpnhlqbttmd --password 'BeBZzfPVtiNXKGUojTK8c8BnEx6G'
SUPABASE_DB_PASSWORD='BeBZzfPVtiNXKGUojTK8c8BnEx6G' supabase db push --linked
# Esperado: applies 0006_v2_schema_columns.sql
```

Se Flávio autorizar, Claude pode rodar isso autonomamente.

## Decisão pós-merge

Próximo prompt a disparar: qualquer um do conjunto {#17, #18, #19, #22} — todos paralelizáveis. Sugestão default: #17 (stint selectors) já que destrava o uso real das colunas pneu_id/combustivel_id.
