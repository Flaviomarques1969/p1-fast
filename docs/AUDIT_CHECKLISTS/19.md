# Audit checklist — PR #19 (Sprint 1A.5 — trechos da pista + seed Brasília)

> Use ao revisar o PR que o Cloud Code abre depois de executar prompt #19.
> **Sem dependência de #16/#17/#18** — `tracks/track_layouts/track_segments` já existem em v1.

## Estrutura

- [ ] Branch `feat/1A5-trechos`, base `main`
- [ ] CI verde
- [ ] Mergeable
- [ ] `Package.resolved` NÃO tocado
- [ ] Sem migration nova (schema já existe em v1_initial)

## Files esperados

**Criados:**
- `ios/p1fast-ios/Sources/Views/TrechoListaView.swift`
- `ios/p1fast-ios/Sources/Persistence/TrackRepository.swift`

**Editados:**
- `ios/p1fast-core/Sources/P1FastCore/Persistence/Models.swift` (+ Track, TrackLayout, TrackSegment, Marco structs)
- `ios/p1fast-core/Sources/P1FastCore/SeedBrasilia.swift` (estendido — NÃO criar arquivo novo)
- `ios/p1fast-ios/Sources/Views/ContentView.swift` (entrypoint + launch arg `--p1-trechos`)
- `ios/p1fast-ios/Sources/Views/GaragemView.swift` (botão "Trechos da pista" no header)

**Files que NÃO devem aparecer:**
- Migrations
- `src/data/` (JS legacy intacto)
- Outros views não-relacionados

## Models (4 structs novos)

- [ ] `struct Track` — id, apelido, nomeOficial, timestamps + Codable + GRDB
- [ ] `struct TrackLayout` — id, trackId, nome, parciais, svgPath, linhaChegada
- [ ] `struct TrackSegment` — id, layoutId, parcialId, ordem, ehTrecho, nome, geometria
- [ ] `struct Marco` — id, layoutId, tipo, posicao, label
- [ ] CodingKeys com snake_case mapping (`track_id`, `layout_id`, `parcial_id`, `eh_trecho`)

## Seed Brasília

- [ ] 1 track "Brasília"
- [ ] 1 layout "Padrão"
- [ ] 8 segments distribuídos em 3 parciais com nomes canônicos:
  - "Curva 01", "Mergulho da Bruxa", "Curva 2", "Junção", "S das Quebradas", "Curva 6", "Cotovelo", "Última Curva"
- [ ] Bootstrap idempotente (count check antes de inserir)

## Visual TrechoListaView

- [ ] Visual <2% diff vs `_design-reference/mockup-trecho-lista.html` (modo CRUD-readonly)
- [ ] Sem radio, sem footer (não é selector aqui)
- [ ] Header eyebrow "Trechos da pista" + título "Brasília" + sub "8 trechos"
- [ ] Group heads "Parcial 1 · Saída do box" / "Parcial 2 · Junção" / etc.
- [ ] `trecho-row` mostra nome + tags (heurística do nome — Lenta/Média/Rápida + Apex)

## Surface

- [ ] Botão "Trechos da pista" no header da `GaragemView` (acima do FAB "Novo carro")
- [ ] Tap abre `TrechoListaView` em push (NavigationStack)

## Smoke

```bash
cd ios/p1fast-core && swift run p1fast-smoke
xcodebuild ... build

DBPATH=$(find ~/Library/Developer/CoreSimulator/Devices -name "*.sqlite" -path "*p1fast*" 2>/dev/null | head -1)
sqlite3 "$DBPATH" "SELECT t.apelido, l.nome, COUNT(s.id) FROM tracks t JOIN track_layouts l ON l.track_id=t.id JOIN track_segments s ON s.layout_id=l.id GROUP BY t.id, l.id;"
# Esperado: Brasília|Padrão|8

git diff main -- ios/p1fast-core/Package.resolved
# Esperado: vazio
```

## Screenshots esperados em `/tmp/p1-trechos/`

- `garagem-com-botao.png`
- `trechos-lista.png`
- `trechos-empty-track.png`

## Decisão pós-merge

Próximo prompt: #20 ou #21 (paralelizáveis). Cada um cria sua migration (0004 lições / 0005 pendências) — números já reservados no HANDOFF.
