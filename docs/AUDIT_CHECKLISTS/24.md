# Audit checklist — PR #24 (Sprint 1A.6 — UI Sincronização)

> Use ao revisar o PR que o Cloud Code abre depois de executar prompt #24 (sub-prompt D).
> **blockedBy:** #23 (precisa do `SyncCoordinator` injetado e da API `@Published`).

## Estrutura

- [ ] Branch `feat/1A6-sync-ui`, base `main` (com #23 mergeado)
- [ ] CI verde
- [ ] Mergeable
- [ ] `Package.resolved` NÃO tocado

## Files esperados

**Criados:**
- `ios/p1fast-ios/Sources/Views/SincronizacaoView.swift`
- `ios/p1fast-ios/Sources/Views/SyncStatusBadge.swift`

**Editados:**
- `ios/p1fast-ios/Sources/Views/HomeView.swift` (+ `SyncStatusBadge` no canto superior direito)
- `ios/p1fast-ios/Sources/Views/ContentView.swift` (entrypoint pra SincronizacaoView)
- (Possível) `ios/p1fast-ios/Sources/Sync/SyncCoordinator.swift` (commit pequeno separado se a API do #23 não bater com o esperado abaixo)

**NÃO devem aparecer:**
- Transports (URLSessionSync/Pull/Telemetry — não tocar)
- Repositories
- Edge Functions
- Migrations / SQL
- p1fast-core/Persistence (lógica pura)

## SyncStatusBadge

- [ ] Pequena badge no canto superior direito do header da `HomeView`
- [ ] 4 estados visuais:
  - 🟢 Verde dot — queue vazia + reachable + última sync < 60s
  - 🟡 Amarelo dot pulsando — drain em andamento OU pending > 0 sem falhas
  - 🔴 Vermelho dot — offline OU dead-letter > 0
  - ⚪ Cinza dot — idle, queue vazia, sem evento recente
- [ ] Tap abre `SincronizacaoView`

## SincronizacaoView

- [ ] Header: "Sincronização" + status pill colorido conforme acima
- [ ] Botão "Sincronizar agora" no header → chama `coordinator.syncNow()`
- [ ] Stat cards: "Última sync OK", "Próxima tentativa", "Items pendentes"
- [ ] Seção "Pendentes" — lista `pendingItems` (table_name + op + age)
- [ ] Seção "Falhas" — lista `deadLetterItems` com botão "Tentar de novo" individual (chama `retryItem(id)`)
- [ ] Swipe-to-delete em items dead-letter, com alert "Apagar definitivamente? Não dá pra desfazer." (chama `dropItem(id)`)
- [ ] Empty state explícito: "Tudo sincronizado." (princípio nunca-fabricar-dados)
- [ ] Sem auto-refresh — `@StateObject`/`@EnvironmentObject` observando coordinator (UI atualiza on-publish)

## API esperada do SyncCoordinator (de #23 ou expandida aqui)

```swift
@MainActor
final class SyncCoordinator: ObservableObject {
    @Published private(set) var status: SyncStatus = .idle
    @Published private(set) var pendingCount: Int = 0
    @Published private(set) var deadLetterCount: Int = 0
    @Published private(set) var lastSuccessAt: Date?
    @Published private(set) var nextAttemptAt: Date?
    @Published private(set) var pendingItems: [SyncQueueItem] = []
    @Published private(set) var deadLetterItems: [SyncQueueItem] = []

    func syncNow() async
    func retryItem(_ id: String) async
    func dropItem(_ id: String) async
}

enum SyncStatus: Equatable { case idle, syncing, offline, hasFailures }
```

- [ ] Se #23 não tinha tudo isso, expandir aqui em commit separado dentro do mesmo PR.

## Smoke (manual)

```bash
cd ios/p1fast-core && swift run p1fast-smoke
xcodebuild ... build

# Sequência E2E manual:
# 1. Desativar wifi do simulator
# 2. Cadastrar 1 piloto → badge VERMELHO, pending=1
# 3. Reativar wifi → drain dispara, badge VERDE, pending=0
# 4. Apontar URL pra inválida temporariamente → ver dead-letter
# 5. Tap "Tentar de novo" → item volta pra pending

git diff main -- ios/p1fast-core/Package.resolved
```

## Screenshots em `/tmp/p1-sync-ui/`

- `home-badge-verde.png`
- `home-badge-amarelo.png`
- `home-badge-vermelho.png`
- `sync-view-pendentes.png`
- `sync-view-deadletter.png`
- `sync-view-tudo-ok.png`

## Decisão pós-merge

**Sprint 1A.6 fechado. Phase 1A 100% completa.**

Próximo: Sprint 1B (cockpit ao vivo) — destravar 5 decisões abertas em `docs/SPRINT_1B_COCKPIT_DESIGN.md` antes de bake dos prompts. CONTROL_CENTER linha 85-92.
