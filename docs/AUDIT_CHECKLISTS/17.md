# Audit checklist — PR #17 (Sprint 1A.4 — stint selectors pneu/combust)

> Use ao revisar o PR que o Cloud Code abre depois de executar prompt #17.
> **blockedBy:** #16 (precisa das colunas `pneu_id`/`combustivel_id`/`qt_combustivel_litros` em `sessoes`).

## Estrutura

- [ ] Branch `feat/1A4-stint-selectors`, base `main` (com #16 já mergeado)
- [ ] CI verde: node-smoke + swift-smoke
- [ ] Mergeable
- [ ] `Package.resolved` NÃO tocado: `git diff main..HEAD --stat -- ios/p1fast-core/Package.resolved` → vazio
- [ ] Sem migration nova (#16 já criou as colunas) — confirmar `git diff main..HEAD --stat -- 'supabase/migrations/*' 'ios/p1fast-core/Sources/P1FastCore/Persistence/Migrations.swift'` → vazio

## Files esperados (e SÓ esses)

**Criados:**
- `ios/p1fast-ios/Sources/Views/PneuPickerView.swift`
- `ios/p1fast-ios/Sources/Views/CombustivelPickerView.swift`

**Editados:**
- `ios/p1fast-ios/Sources/Views/StintModalView.swift`
- `ios/p1fast-ios/Sources/Persistence/StintRepository.swift`

**Files que NÃO devem aparecer no diff:**
- Migrations / SQL
- PneuRepository, CombustivelRepository, PilotoRepository (sem mudança esperada — pickers consomem read-only)
- Outras views

## Pickers (visual + comportamento)

- [ ] `PneuPickerView` recebe `carroId` + `selectedId: Binding<String?>`. Lista só pneus daquele carro (ou empty state com CTA pra `PneuCadastroView`).
- [ ] `CombustivelPickerView` recebe `selectedId: Binding<String?>`. Lista todos os combust do `time_id` (não escopa por carro). Empty → CTA pra `CombustivelCadastroView`.
- [ ] Ambos têm radio + footer "Voltar / Confirmar" (uso original dos mockups).
- [ ] Visual <2% diff vs `mockup-pneu-lista.html` e `mockup-combustivel-lista.html`.

## StintModalView (campos novos)

- [ ] 2 campos novos na seção "Configuração" (linhas ~315 antes do PR):
  - "Combustível abastecido" — input qty (litros, 1 decimal, opcional) + tap abre `CombustivelPickerView`.
  - "Pneu montado" — tap abre `PneuPickerView` escopado pelo carro do stint.
- [ ] Layout 1:1 com `mockup-stint.html` linhas 303-323.
- [ ] Stint 'completa' → campos read-only no UI (não bloquear no banco).

## StintRepository (3 métodos novos)

- [ ] `setPneu(stintId:pneuId:)` — atualiza `sessoes.pneu_id`.
- [ ] `setCombustivel(stintId:combustivelId:litros:)` — atualiza ambas colunas.
- [ ] `incrementarCiclos(pneuId:by:)` — soma em `pneus.ciclos`.
- [ ] `finalizarStint(...)` chama `incrementarCiclos` automaticamente quando `pneu_id != NULL` (somando `voltas.count`).

## Smoke local

```bash
cd ios/p1fast-core && swift run p1fast-smoke
# Esperado: 158/0 (ou +N se adicionou testes pra incrementarCiclos)
```

## Smoke persistência

```bash
DBPATH=$(find ~/Library/Developer/CoreSimulator/Devices -name "*.sqlite" -path "*p1fast*" 2>/dev/null | head -1)

# Após criar stint pelo simulator com pneu + combust + qty:
sqlite3 "$DBPATH" "SELECT id, pneu_id, combustivel_id, qt_combustivel_litros FROM sessoes WHERE status='planejada' ORDER BY created_at DESC LIMIT 1;"
# Esperado: row com FKs preenchidas + qty

# Após finalizar stint com 5 voltas:
PNEU_ID=$(sqlite3 "$DBPATH" "SELECT pneu_id FROM sessoes WHERE id='<stint_id>';")
sqlite3 "$DBPATH" "SELECT ciclos FROM pneus WHERE id='$PNEU_ID';"
# Esperado: ciclos += 5
```

## Screenshots esperados em `/tmp/p1-stint-selectors/`

- `stint-modal-pickers-vazio.png`
- `pneu-picker-empty.png`
- `pneu-picker-com-2.png`
- `combust-picker-com-2.png`
- `stint-modal-preenchido.png`

## Decisão pós-merge

Próximo prompt: #18 (pessoas v2) — independente, paralelizável com qualquer outro do conjunto {#19, #22}.
