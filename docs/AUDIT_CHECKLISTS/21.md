# Audit checklist — PR #21 (Sprint 1A.5 — pendências cascata)

> Use ao revisar o PR que o Cloud Code abre depois de executar prompt #21.
> **Paralelizável com #20.** Cria migration nova (0005 + GRDB v5_pendencias).

## Estrutura

- [ ] Branch `feat/1A5-pendencias`, base `main`
- [ ] CI verde
- [ ] Mergeable
- [ ] `Package.resolved` NÃO tocado

## Migration naming

- [ ] GRDB: `m.registerMigration("v5_pendencias")` — DEPOIS de `v4_licoes` (se #20 mergeou primeiro) OU pode ser antes (ordem de aplicação não importa pra esquemas isolados — GRDB roda na ordem de registro do código)
- [ ] Postgres: `supabase/migrations/0005_pendencias.sql` (número reservado)

## Files esperados

**Criados:**
- `ios/p1fast-ios/Sources/Views/PendenciasView.swift`
- `ios/p1fast-ios/Sources/Persistence/PendenciaRepository.swift`
- `supabase/migrations/0005_pendencias.sql`

**Editados:**
- `ios/p1fast-core/Sources/P1FastCore/Persistence/Migrations.swift` (+ registerMigration `v5_pendencias`)
- `ios/p1fast-core/Sources/P1FastCore/Persistence/Models.swift` (+ structs PendenciaTemplate, EventoPendencia)
- `ios/p1fast-ios/Sources/Views/EventoDetalheView.swift` (seção "Pendências" com link pra PendenciasView)

**NÃO devem aparecer:**
- DB.swift
- 0004 ou outras migrations
- `src/data/schemas.js` (legacy intacto)

## Schema

```sql
CREATE TABLE pendencias_template (
    id TEXT PRIMARY KEY,
    grupo_id TEXT NOT NULL,
    grupo_titulo TEXT NOT NULL,
    grupo_num TEXT NOT NULL,
    titulo TEXT NOT NULL,
    observacao TEXT,
    obrigatorio INTEGER NOT NULL DEFAULT 0,
    ordem INTEGER NOT NULL,
    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL,
    synced_at INTEGER
);

CREATE TABLE evento_pendencias (
    id TEXT PRIMARY KEY,
    evento_id TEXT NOT NULL REFERENCES eventos(id) ON DELETE CASCADE,
    template_id TEXT NOT NULL REFERENCES pendencias_template(id) ON DELETE CASCADE,
    checado INTEGER NOT NULL DEFAULT 0,
    checado_at INTEGER,
    nota TEXT,
    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL,
    synced_at INTEGER
);
CREATE INDEX idx_evento_pendencias_evento ON evento_pendencias(evento_id);
CREATE UNIQUE INDEX idx_evento_pendencias_unique ON evento_pendencias(evento_id, template_id);
```

## Seed canônico

- [ ] Portado de `src/data/schemas.js` → `SEED_PEND_TIPOS`
- [ ] Confirmar contagem com:
  ```bash
  grep -cE "^\s+\{" src/data/schemas.js  # ajustar regex se necessário
  ```
- [ ] ~6-7 grupos, ~30 itens totais (Motor & Fluidos, Freios, Pneus & Susp., Elétrica, Segurança, Torques Susp., Combustível)
- [ ] Bootstrap idempotente

## Comportamento

- [ ] Templates seedados 1x no boot (`PendenciaRepository.bootstrap()`)
- [ ] Instâncias por evento criadas auto na 1ª abertura da PendenciasView pra esse evento (N inserts em `evento_pendencias`)
- [ ] Grupos colapsáveis. Header mostra "{X}/{N} checadas" + cor (verde 100%, ouro >0, faint 0)
- [ ] Itens `obrigatorio=1` ganham asterisco vermelho ou tag "obrig."
- [ ] Tap longo no item → sheet textarea pra `nota`
- [ ] UNIQUE constraint `(evento_id, template_id)` previne duplicação

## Smoke

```bash
cd ios/p1fast-core && swift run p1fast-smoke
xcodebuild ... build

DBPATH=$(find ~/Library/Developer/CoreSimulator/Devices -name "*.sqlite" -path "*p1fast*" 2>/dev/null | head -1)
sqlite3 "$DBPATH" "SELECT COUNT(*) FROM pendencias_template;"
# Esperado: ~30

# Após criar 1 evento + abrir PendenciasView:
EID=$(sqlite3 "$DBPATH" "SELECT id FROM eventos ORDER BY created_at DESC LIMIT 1;")
sqlite3 "$DBPATH" "SELECT COUNT(*) FROM evento_pendencias WHERE evento_id='$EID';"
# Esperado: igual ao count de templates

# Após marcar item:
sqlite3 "$DBPATH" "SELECT checado FROM evento_pendencias WHERE evento_id='$EID' AND template_id='<tid>';"
# Esperado: 1
```

## Screenshots em `/tmp/p1-pendencias/`

- `pendencias-vazias.png`
- `pendencias-meio.png`
- `pendencias-completo.png`
- `pendencias-nota.png`

## Action pós-merge

Aplicar 0005 em prod Supabase manualmente.

## Decisão pós-merge

Próximo: #22 (setup avançado) — sem schema novo, refactor visual puro.
